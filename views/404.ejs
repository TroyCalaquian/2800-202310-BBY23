<%- include('./templates/htmlOpen') %>

<!-- Stylesheets Added Here -->

<%- include('./templates/header') %>

<h1>Sorry, page "<span id="badInput">Page</span>" does not exist.</h1>

<%- include('./templates/footer') %>

<!-- js scripts added here -->
    function overrideHeader() {
    // Copy URL
    var urlPath = window.location.pathname;
    var urlParts = urlPath.split('/');
    
    // Get error from URL
    var erroneousInput = urlParts[urlParts.length - 1];
    
    // Test. Prints to log
    console.log("Bad Input: " + erroneousInput);

    // Get span by ID
    var badInput = document.getElementById("badInput");
    
    // Override html found earlier
    badInput.innerHTML = erroneousInput;
    }
    overrideHeader();
</script> -->

<script type="text/javascript" src="../scripts/displayErrorURL.js"></script>
<%- include('./templates/htmlClose') %>
